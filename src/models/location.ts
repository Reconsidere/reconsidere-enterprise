export class Location {

  country: string;
  state: string;
  latitude: number;
  longitude: number;
  cep: string;
  publicPlace: string;
  neighborhood: string;
  number: number;
  county: string;
  complement: string;

  constructor(country: string, state: string, latitude: number, longitude: number, cep: string, publicPlace: string, neighborhood: string, number: number,county:string, complement:string) {}

}
