import { BooltransformPipe } from './booltransform.pipe';

describe('BooltransformPipe', () => {
  it('create an instance', () => {
    const pipe = new BooltransformPipe();
    expect(pipe).toBeTruthy();
  });
});
