import { TermFilterPipe } from './term-filter.pipe';

describe('TermFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TermFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
